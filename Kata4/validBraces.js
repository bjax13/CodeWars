function validBraces(braces){
  var arr = braces.split('');
  var leftStack = [];
  var rightStack = [];

  var leftBraces = ['(', '{', '['];
  var rightBraces = [')', '}', '] '];

  for (var i = 0; i < arr.length; i++) {
    arr[i]
  }

  return true;
}
